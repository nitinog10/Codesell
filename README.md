# nitinog10/Codesell — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

```
The `repo_d7d98402c220` repository is a full-stack web application built using Next.js, TypeScript, and Prisma. The app facilitates the buying and selling of code repositories, with features for user authentication, admin management, and payment processing via Stripe and Razorpay. 

The tech stack includes Next.js for the frontend framework, TypeScript for type safety, Prisma as the ORM for database interactions, and Tailwind CSS for styling. The project is structured to separate concerns, with directories for API routes (`src/app/api`), frontend pages (`src/app`), and reusable components (`src/components`).

Users of this application include buyers looking to purchase code repositories, sellers listing their code for sale, and admins managing products and orders. The app provides a seamless experience for browsing, purchasing, and managing code repositories.
```

## Architecture

```markdown
## Architecture

### Overview

The codebase is structured to support a scalable and maintainable web application. It is organized into distinct layers and modules, each serving a specific purpose. The primary technologies used are Next.js for the frontend, Prisma for database interactions, and various utility libraries for common tasks.

### Layers and Modules

1. **Configuration and Setup**
   - `.env.example`: Example environment variables.
   - `eslint.config.mjs`: ESLint configuration.
   - `next-env.d.ts`: TypeScript definitions for Next.js.
   - `next.config.ts`: Next.js configuration.
   - `postcss.config.mjs`: PostCSS configuration.
   - `tailwind.config.ts`: Tailwind CSS configuration.
   - `tsconfig.json`: TypeScript configuration.

2. **Emails**
   - `emails/`: Contains React components for generating email templates.
     - `AdminAlert.tsx`
     - `CollabInvite.tsx`
     - `PurchaseConfirmation.tsx`

3. **Database**
   - `prisma/`: Prisma schema and seed files.
     - `schema.prisma`: Defines the database schema.
     - `seed.ts`: Initializes the database with sample data.

4. **Source Code**
   - `src/`: Main application code.
     - `middleware.ts`: Middleware for session-based authentication.
     - `app/`: Next.js pages and API routes.
       - `error.tsx`, `globals.css`, `layout.tsx`, etc.: Common UI components and configurations.
       - `admin/`: Admin-specific pages and components.
       - `api/`: API routes for various functionalities.
         - `admin/`, `auth/`, `orders/`, `webhooks/`: Specific API route handlers.
       - `auth/`, `checkout/`, `dashboard/`, `products/`: User-facing pages and components.
     - `components/`: Reusable UI components.
       - `admin/`, `auth/`, `checkout/`, `dashboard/`, `products/`, `ui/`: Categorized by feature or purpose.
     - `lib/`: Utility functions and libraries.
       - `auth.ts`, `email.tsx`, `encryption.ts`, etc.: Various utility modules.

### Data Flow

1. **User Interaction**: Users interact with the application through the frontend pages (`/src/app/`).
2. **API Requests**: User actions trigger API requests handled by routes defined in `/src/app/api/`.
3. **Business Logic**: API routes perform business logic, often utilizing utility functions from `/src/lib/`.
4. **Database Operations**: Prisma client in `/src/lib/prisma.ts` interacts with the database based on the schema defined in `prisma/schema.prisma`.
5. **Response**: The API routes send responses back to the frontend, which updates the UI accordingly.

### Key Design Patterns

- **MVC (Model-View-Controller)**: Separation of concerns between data models (`prisma/`), views (`src/app/` pages), and controllers (`src/app/api/` routes).
- **Utility Modules**: Centralized logic in `/src/lib/` to promote code reuse and maintainability.
- **Component-Based UI**: Reusable UI components in `/src/components/` to ensure consistency and modularity.

### Main Entry Points

- **Frontend**: `src/app/page.tsx` serves as the main entry point for the Next.js application.
- **API**: `src/app/api/` contains the entry points for various API functionalities.
- **Middleware**: `src/middleware.ts` enforces session-based authentication across the application.
```

## Folder Structure

```
├── .env.example
├── README.md
├── eslint.config.mjs
├── next-env.d.ts
├── next.config.ts
├── package.json
├── plan.md
├── postcss.config.mjs
├── setup.md
├── tailwind.config.ts
├── tsconfig.json
├── emails/
│   ├── AdminAlert.tsx
│   ├── CollabInvite.tsx
│   └── PurchaseConfirmation.tsx
├── prisma/
│   ├── schema.prisma
│   └── seed.ts
└── src/
    ├── middleware.ts
    ├── app/
    │   ├── error.tsx
    │   ├── globals.css
    │   ├── layout.tsx
    │   ├── loading.tsx
    │   ├── not-found.tsx
    │   ├── page.tsx
    │   ├── robots.ts
    │   ├── sitemap.ts
    │   ├── admin/
    │   │   ├── page.tsx
    │   │   ├── orders/
    │   │   │   └── page.tsx
    │   │   └── products/
    │   │       ├── page.tsx
    │   │       ├── [id]/
    │   │       │   └── edit/
    │   │       │       └── page.tsx
    │   │       └── new/
    │   │           └── page.tsx
    │   ├── api/
    │   │   ├── admin/
    │   │   │   └── products/
    │   │   │       ├── route.ts
    │   │   │       └── [id]/
    │   │   │           └── route.ts
    │   │   ├── auth/
    │   │   │   └── [...nextauth]/
    │   │   │       └── route.ts
    │   │   ├── orders/
    │   │   │   ├── [id]/
    │   │   │   │   └── retry-collab/
    │   │   │   │       └── route.ts
    │   │   │   └── create/
    │   │   │       └── route.ts
    │   │   └── webhooks/
    │   │       ├── razorpay/
    │   │       │   └── route.ts
    │   │       └── stripe/
    │   │           └── route.ts
    │   ├── auth/
    │   │   └── signin/
    │   │       └── page.tsx
    │   ├── checkout/
    │   │   ├── page.tsx
    │   │   ├── failure/
    │   │   │   └── page.tsx
    │   │   └── success/
    │   │       └── page.tsx
    │   ├── dashboard/
    │   │   ├── page.tsx
    │   │   └── orders/
    │   │       ├── page.tsx
    │   │       └── [id]/
    │   │           └── page.tsx
    │   └── products/
    │       ├── page.tsx
    │       └── [slug]/
    │           └── page.tsx
    ├── components/
    │   ├── admin/
    │   │   ├── ProductActions.tsx
    │   │   └── ProductForm.tsx
    │   ├── auth/
    │   │   └── AuthButton.tsx
    │   ├── checkout/
    │   │   └── PaymentForm.tsx
    │   ├── dashboard/
    │   │   ├── OrderList.tsx
    │   │   ├── PurchasedRepos.tsx
    │   │   └── RetryCollabButton.tsx
    │   ├── layout/
    │   │   ├── Footer.tsx
    │   │   └── Navbar.tsx
    │   ├── products/
    │   │   ├── ProductCard.tsx
    │   │   ├── ProductCatalog.tsx
    │   │   └── ProductDetail.tsx
    │   └── ui/
    │       ├── Badge.tsx
    │       ├── Button.tsx
    │       ├── Input.tsx
    │       ├── StatusPill.tsx
    │       └── Textarea.tsx
    ├── lib/
    │   ├── auth.ts
    │   ├── email.tsx
    │   ├── encryption.ts
    │   ├── env.ts
    │   ├── fulfillment.ts
    │   ├── github.ts
    │   ├── guards.ts
    │   ├── orders.ts
    │   ├── prisma.ts
    │   ├── products.ts
    │   ├── razorpay.ts
    │   ├── sample-data.ts
    │   ├── stripe.ts
    │   ├── utils.ts
    │   └── validators.ts
    └── types/
        └── next-auth.d.ts
```

## Dependencies

## Dependencies

### Major Libraries

#### Next.js
- **Purpose**: A React framework for production.
- **Version Constraint**: `^15.3.3`
- **Type**: Production

#### React
- **Purpose**: A JavaScript library for building user interfaces.
- **Version Constraint**: `^19.0.0`
- **Type**: Production

#### React-DOM
- **Purpose**: DOM bindings for React.
- **Version Constraint**: `^19.0.0`
- **Type**: Production

#### Prisma
- **Purpose**: An ORM for Node.js and TypeScript.
- **Version Constraint**: `^6.8.2`
- **Type**: Production

#### Tailwind CSS
- **Purpose**: A utility-first CSS framework for rapidly building custom designs.
- **Version Constraint**: `^4.1.8`
- **Type**: Production

#### TypeScript
- **Purpose**: A typed superset of JavaScript that compiles to plain JavaScript.
- **Version Constraint**: `^5.7.2`
- **Type**: Development

#### ESLint
- **Purpose**: A tool for identifying and reporting on patterns found in ECMAScript/JavaScript code.
- **Version Constraint**: `^9.17.0`
- **Type**: Development

#### @auth/prisma-adapter
- **Purpose**: An adapter for using Prisma with NextAuth.js.
- **Version Constraint**: `^2.10.0`
- **Type**: Production

#### @octokit/rest
- **Purpose**: A REST client for the GitHub API.
- **Version Constraint**: `^21.1.1`
- **Type**: Production

#### @prisma/client
- **Purpose**: Prisma Client for database access.
- **Version Constraint**: `^6.8.2`
- **Type**: Production

#### @radix-ui/react-slot
- **Purpose**: A component for rendering children with specific props.
- **Version Constraint**: `^1.1.1`
- **Type**: Production

#### @react-email/components
- **Purpose**: Components for building email templates with React.
- **Version Constraint**: `^0.0.38`
- **Type**: Production

#### class-variance-authority
- **Purpose**: Utility for managing class names in a type-safe way.
- **Version Constraint**: `^0.7.1`
- **Type**: Production

#### clsx
- **Purpose**: A utility for constructing className strings conditionally.
- **Version Constraint**: `^2.1.1`
- **Type**: Production

#### lucide-react
- **Purpose**: React icons based on Lucide.
- **Version Constraint**: `^0.468.0`
- **Type**: Production

#### next-auth
- **Purpose**: Authentication for Next.js applications.
- **Version Constraint**: `^5.0.0-beta.28`
- **Type**: Production

#### resend
- **Purpose**: A library for sending emails.
- **Version Constraint**: `^4.5.1`
- **Type**: Production

#### stripe
- **Purpose**: A library for handling payments.
- **Version Constraint**: `^17.7.0`
- **Type**: Production

#### tailwind-merge
- **Purpose**: A utility for merging Tailwind CSS classes.
- **Version Constraint**: `^2.6.0`
- **Type**: Production

#### zod
- **Purpose**: A TypeScript-first schema declaration and validation library.
- **Version Constraint**: `^3.24.1`
- **Type**: Production

### Development Dependencies

#### @eslint/eslintrc
- **Purpose**: ESLint configuration for TypeScript projects.
- **Version Constraint**: `^3.3.1`
- **Type**: Development

#### @tailwindcss/postcss
- **Purpose**: PostCSS plugin for Tailwind CSS.
- **Version Constraint**: `^4.1.8`
- **Type**: Development

#### @types/node
- **Purpose**: TypeScript definitions for Node.js.
- **Version Constraint**: `^22.10.2`
- **Type**: Development

#### @types/react
- **Purpose**: TypeScript definitions for React.
- **Version Constraint**: `^19.0.2`
- **Type**: Development

#### @types/react-dom
- **Purpose**: TypeScript definitions for React DOM.
- **Version Constraint**: `^19.0.2`
- **Type**: Development

#### eslint-config-next
- **Purpose**: ESLint configuration for Next.js.
- **Version Constraint**: `^15.3.3`
- **Type**: Development

#### tsx
- **Purpose**: A TypeScript version of Babel's `ts-node`.
- **Version Constraint**: `^4.19.2`
- **Type**: Development

## File Documentation

### .env.example

#### Overview

# `.env.example` Documentation

This file contains example environment variables used to configure the application's settings. It's a template for developers to create their own `.env` file with actual values.

#### Dependencies

No direct dependencies are listed here, but the actual `.env` file will be read by various parts of the application to configure services like authentication, database connections, payment providers, and email services.

#### Configuration

| Key | Purpose | Example Value |
| --- | --- | --- |
| `NEXT_PUBLIC_APP_URL` | Public URL for the application. | `http://localhost:3000` |
| `NEXTAUTH_URL` | URL for NextAuth.js configuration. | `http://localhost:3000` |
| `AUTH_SECRET` | Secret key for authentication. | `replace-with-a-random-32-character-secret` |
| `NEXTAUTH_SECRET` | Secret key for NextAuth.js. | `replace-with-a-random-32-character-secret` |
| `DATABASE_URL` | URL for the database connection. | `postgresql://user:pass@host/dbname?sslmode=require` |
| `GITHUB_CLIENT_ID` | Client ID for GitHub OAuth. | `replace-with-github-oauth-client-id` |
| `GITHUB_CLIENT_SECRET` | Client secret for GitHub OAuth. | `replace-with-github-oauth-client-secret` |
| `GITHUB_SELLER_PAT` | Personal access token for GitHub. | `replace-with-seller-fine-grained-pat` |
| `PAYMENT_PROVIDER` | Payment provider to use (e.g., Stripe, Razorpay). | `stripe` |
| `STRIPE_SECRET_KEY` | Secret key for Stripe. | `sk_test_replace` |
| `STRIPE_PUBLISHABLE_KEY` | Publishable key for Stripe. | `pk_test_replace` |
| `STRIPE_WEBHOOK_SECRET` | Webhook secret for Stripe. | `whsec_replace` |
| `RAZORPAY_KEY_ID` | Key ID for Razorpay. | `rzp_test_replace` |
| `RAZORPAY_KEY_SECRET` | Key secret for Razorpay. | `replace-with-razorpay-secret` |
| `RAZORPAY_WEBHOOK_SECRET` | Webhook secret for Razorpay. | `replace-with-razorpay-webhook-secret` |
| `RESEND_API_KEY` | API key for Resend email service. | `re_replace` |
| `EMAIL_FROM` | From address for emails. | `CodeSell <delivered@codesell.dev>` |
| `ADMIN_EMAIL` | Admin email address. | `owner@example.com` |
| `ENCRYPTION_KEY` | Encryption key for sensitive data. | `replace-with-64-hex-characters` |

#### Notes

- All placeholder values in this file must be replaced with actual credentials and configuration values before using the application.
- Sensitive information like secrets and API keys should be stored securely and not committed to version control.
- The `ENCRYPTION_KEY` should be a 64-character hexadecimal string for secure data encryption.
- Ensure the `DATABASE_URL` is correctly formatted for your database service provider.
- The `PAYMENT_PROVIDER` should match the actual payment provider being used (e.g., `stripe` or `razorpay`).

### README.md

#### Overview

# Module Overview

This `README.md` file provides essential setup and usage instructions for the `CodeSell` project. It details how to set up the local environment, dependencies, and key components of the application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| Next.js | Framework for server-rendered React applications |
| NextAuth | Authentication library with GitHub provider |
| Prisma | ORM for PostgreSQL |
| Stripe | Payment processing |
| Razorpay | Payment processing |
| Octokit | GitHub API client |
| Resend | Email notifications |
| Tailwind CSS | Utility-first CSS framework |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| User | Represents a user in the system | `getByEmail`, `updateRole` |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| setupDatabase | None | None | Initializes the database with schema and seed data |
| authenticateUser | `email`, `password` | `User` object | Authenticates a user and returns user details |

#### Configuration

| Environment Variable | Purpose |
| --- | --- |
| `DATABASE_URL` | PostgreSQL connection string |
| `GITHUB_SELLER_PAT` | GitHub Personal Access Token for repository delivery |
| `STRIPE_SECRET_KEY` | Stripe secret key for payment processing |
| `RAZORPAY_KEY` | Razorpay API key for payment processing |
| `RESEND_API_KEY` | Resend API key for email notifications |

#### Notes

- Admin access is controlled by the `User.role` field. Set your user to `ADMIN` in the database after the first GitHub login.
- The public catalog falls back to sample products when `DATABASE_URL` is not configured, but order creation requires a real database.
- Repository delivery uses `GITHUB_SELLER_PAT` and always requests GitHub `pull` permission.

### eslint.config.mjs

#### Overview

# eslint.config.mjs

This file configures ESLint for our project, setting up the rules and extensions we use for linting JavaScript and TypeScript files. It ensures consistent code quality by integrating Next.js and TypeScript-specific rules.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `FlatCompat` | Provides compatibility for ESLint configuration files. |
| `dirname` | Retrieves the directory name from a file path. |
| `fileURLToPath` | Converts a URL to a file path. |

#### Configuration

| Configuration | Purpose |
| --- | --- |
| `ignores` | Specifies directories and files that ESLint should ignore. |
| `extends` | Integrates predefined sets of rules from Next.js and TypeScript. |

#### Notes

- The `ignores` array ensures that certain directories and files, like build outputs and Next.js caches, are not linted.
- The `extends` method incorporates Next.js and TypeScript-specific rules to maintain consistency across our codebase.

### next-env.d.ts

#### Overview

# next-env.d.ts

This file provides TypeScript type definitions for Next.js, ensuring type safety and IntelliSense for Next.js-specific features in our project.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `next` | Types for Next.js core functionalities. |
| `next/image-types/global` | Types specific to Next.js image optimization features. |
| `./.next/types/routes.d.ts` | Types for route definitions in Next.js. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| N/A | N/A | N/A | N/A |

#### Configuration or Constants

| Identifier | Purpose |
| --- | --- |
| N/A | N/A |

#### Notes

- This file should not be edited directly. It is auto-generated and maintained by Next.js.
- For more information, refer to the [Next.js TypeScript documentation](https://nextjs.org/docs/app/api-reference/config/typescript).

### next.config.ts

#### Overview

# next.config.ts

This file configures the Next.js framework for our project, enabling specific experimental features.

#### Dependencies

- `NextConfig`: Imported from the `next` package, this type defines the structure of the Next.js configuration object.

#### Configuration

| Configuration | Purpose | Key Methods |
|---------------|---------|-------------|
| `experimental` | Enables experimental features in Next.js. | N/A |
| `serverActions` | Allows configuration of server actions, including body size limits. | N/A |
| `bodySizeLimit` | Sets the maximum allowed size of the request body for server actions. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| N/A | N/A | N/A | This file does not define any functions. |

#### Notes

- The `bodySizeLimit` is set to `2mb`, which means server actions will reject requests with a body larger than 2 megabytes.
- This configuration enables experimental features, which may change or be removed in future versions of Next.js.

### package.json

#### Overview

```markdown
# package.json

#### Module Overview

The `package.json` file is the heart of any Node.js project. It holds metadata about the project, including its name, version, dependencies, and scripts for running various tasks.

#### Dependencies

| Dependency | Purpose |
|------------|---------|
| `next` | Framework for building the application. |
| `react` | Library for building user interfaces. |
| `prisma` | ORM for database interactions. |
| `stripe` | Library for handling payments. |
| `tailwindcss` | Utility-first CSS framework for styling. |

#### Scripts

| Script | Command | Description |
|--------|---------|-------------|
| `dev` | `next dev` | Starts the development server. |
| `build` | `next build` | Builds the application for production. |
| `start` | `next start` | Starts the production server. |
| `lint` | `eslint.` | Runs linting on the codebase. |
| `db:generate` | `prisma generate` | Generates Prisma client. |
| `db:push` | `prisma db push` | Pushes migrations to the database. |
| `db:migrate` | `prisma migrate dev` | Creates and applies new migrations. |
| `db:seed` | `prisma db seed` | Seeds the database with initial data. |
| `postinstall` | `prisma generate` | Generates Prisma client after installing dependencies. |

#### Configuration

| Key | Value | Description |
|-----|-------|-------------|
| `prisma.seed` | `tsx prisma/seed.ts` | Command to run for seeding the database. |

#### Notes

- Ensure you run `npm install` or `yarn install` after modifying `package.json`.
- The `postinstall` script ensures the Prisma client is generated after every dependency installation.
- Always update the version numbers of dependencies to the latest stable versions to avoid security vulnerabilities.
```

### plan.md

#### Overview

# CodeSell — MVP Implementation Plan

#### Module Overview

This file outlines the implementation plan for CodeSell, an e-commerce platform for selling source code with GitHub-native authentication and automated repository access delivery.

#### Dependencies

- **Next.js**: Full-stack framework for frontend and API routes.
- **TypeScript**: Type safety across the stack.
- **Tailwind CSS + shadcn/ui**: Rapid, consistent UI styling.
- **NextAuth.js**: GitHub OAuth with minimal boilerplate.
- **Prisma**: Type-safe database queries and migrations.
- **Octokit.js**: Official GitHub SDK for collaborator management.
- **Resend + React Email**: Transactional emails with React templates.

#### Classes

None

#### Functions

None

#### Configuration

- **GitHub OAuth App scopes**: `read:user`, `user:email`
- **Seller's GitHub PAT scopes**: `repo`, `admin:org`
- **NextAuth callbacks**: `jwt`, `session`, `signIn`

#### Constants

None

#### Notes

- The seller's GitHub account uses a **Personal Access Token (PAT)** with `repo` + `admin:org` scope to add collaborators. This is separate from buyer tokens.
- Webhook security: Verify Stripe and Razorpay signatures, ensure idempotency, and implement retry logic for GitHub API failures.

### postcss.config.mjs

#### Overview

# postcss.config.mjs

This file configures PostCSS plugins for the project. It defines the plugins to be used in the PostCSS processing pipeline, specifically including Tailwind CSS for utility-first CSS framework.

#### Dependencies

- **Tailwind CSS**: A utility-first CSS framework for rapidly building custom user interfaces.

#### Configuration

| Configuration | Purpose | 
|---------------|---------|
| `plugins` | Defines the list of PostCSS plugins to be used. |

#### Notes

- This configuration is minimal, including only Tailwind CSS.
- Ensure Tailwind CSS is installed in the project dependencies to avoid build errors.
- For more complex setups, additional plugins can be added to the `plugins` object.

### setup.md

#### Overview

# CodeSell Setup Guide

This guide takes the app from a fresh clone to a working local MVP, then lists the production settings needed for real GitHub repository delivery.

#### 1. Requirements

- Node.js 22 or newer
- npm 11 or newer
- PostgreSQL database, for example Neon
- GitHub OAuth app for buyer login
- GitHub seller Personal Access Token for repository collaborator invites
- Stripe or Razorpay account
- Resend account for transactional email

#### 2. Install Dependencies

```bash
npm install
```

This also runs `prisma generate` through the `postinstall` script.

#### 3. Create Local Environment File

Copy the example file:

```bash
copy.env.example.env.local
```

Generate local secrets:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Use the base64url value for `AUTH_SECRET` and `NEXTAUTH_SECRET`. Use the hex value for `ENCRYPTION_KEY`.

Minimum local `.env.local` values:

```env
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
AUTH_SECRET=replace-with-generated-secret
NEXTAUTH_SECRET=replace-with-generated-secret
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
ENCRYPTION_KEY=replace-with-64-character-hex-key
```

The public catalog can render with sample products before the database is configured, but order creation, admin CRUD, dashboards, webhooks, and delivery require `DATABASE_URL`.

#### 4. Configure GitHub OAuth

Create a GitHub OAuth app:

- Homepage URL: `http://localhost:3000`
- Authorization callback URL: `http://localhost:3000/api/auth/callback/github`

Add the values:

```env
GITHUB_CLIENT_ID=replace-with-github-oauth-client-id
GITHUB_CLIENT_SECRET=replace-with-github-oauth-client-secret
```

The app requests `read:user` and `user:email` scopes.

#### 5. Configure Seller Repository Delivery

Create a GitHub seller Personal Access Token that can add collaborators to the private product repositories.

Add the values:

```env
GITHUB_SELLER_PAT=replace-with-seller-pat
GITHUB_SELLER_USERNAME=replace-with-seller-github-username
```

The app calls GitHub with `permission: "pull"` so buyers only receive read-only access.

#### 6. Set Up Database

Push the schema:

```bash
npm run db:push
```

Seed sample products:

```bash
npm run db:seed
```

For migration-based development, use:

```bash
npm run db:migrate
```

Validate the schema:

```bash
npx prisma validate
```

If Prisma validation runs before `.env.local` has `DATABASE_URL`, it will fail because the schema requires that environment variable.

#### 7. Bootstrap Admin Access

1. Start the app.
2. Login once with GitHub at `/auth/signin`.
3. Update your user role in the database from `BUYER` to `ADMIN`.

Example SQL:

```sql
UPDATE "User"
SET role = 'ADMIN'
WHERE email = 'you@example.com';
```

After this, `/admin`, `/admin/products`, and `/admin/orders` are available.

#### 8. Configure Payments

Choose one provider with `PAYMENT_PROVIDER`.

Stripe:

```env
PAYMENT_PROVIDER=stripe
STRIPE_SECRET_KEY=sk_test_replace
STRIPE_PUBLISHABLE_KEY=pk_test_replace
STRIPE_WEBHOOK_SECRET=whsec_replace
```

Webhook endpoint:

```text
http://localhost:3000/api/webhooks/stripe
```

Stripe events handled:

- `checkout.session.completed`
- `checkout.session.async_payment_failed`
- `charge.refunded`

Razorpay:

```env
PAYMENT_PROVIDER=razorpay
RAZORPAY_KEY_ID=rzp_test_replace
RAZORPAY_KEY_SECRET=replace-with-razorpay-secret
RAZORPAY_WEBHOOK_SECRET=replace-with-razorpay-webhook-secret
```

Webhook endpoint:

```text
http://localhost:3000/api/webhooks/razorpay
```

Razorpay events handled:

- `payment.captured`
- `order.paid`

In non-production, if no provider is configured, the app can return a mock checkout success URL after creating an order.

#### 9. Configure Email

Add Resend settings:

```env
RESEND_API_KEY=re_replace
EMAIL_FROM=CodeSell <delivered@codesell.dev>
ADMIN_EMAIL=owner@example.com
```

Emails are sent for purchase confirmation, collaborator invite notice, and admin delivery failure alerts. If `RESEND_API_KEY` is missing in development, email delivery is skipped and logged.

#### 10. Run Locally

```bash
npm run dev
```

Open:

```text
http://localhost:3000
```

Useful routes:

- `/products`
- `/auth/signin`
- `/checkout`
- `/dashboard`
- `/admin`
- `/admin/products`
- `/admin/orders`

#### 11. Verify Before Launch

Run:

```bash
npm run lint
npm run build
```

Expected note: the current code may warn about raw `<img>` usage for dynamic product image URLs. The build still passes.

#### 12. Production Checklist

- Set `NEXT_PUBLIC_APP_URL` and `NEXTAUTH_URL` to the production domain.
- Use strong production-only `AUTH_SECRET`, `NEXTAUTH_SECRET`, and `ENCRYPTION_KEY`.
- Use a production PostgreSQL connection string.
- Update the GitHub OAuth callback URL to:

  ```text
  https://your-domain.com/api/auth/callback/github
  ```

- Add production payment webhook endpoints:

  ```text
  https://your-domain.com/api/webhooks/stripe
  https://your-domain.com/api/webhooks/razorpay
  ```

- Use live payment keys only after test-mode checkout and webhook delivery pass.
- Confirm the seller GitHub PAT can add collaborators to every product repo.
- Confirm `ADMIN_EMAIL` receives delivery failure alerts.
- Set your admin user role to `ADMIN`

### tailwind.config.ts

#### Overview

# tailwind.config.ts

#### Module Overview

This file defines the Tailwind CSS configuration for our project. It specifies which files to scan for class names and sets up the theme's customizations.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Config` | Type definition for Tailwind CSS configuration. |

#### Configuration

| Key | Value | Description |
| --- | --- | --- |
| `content` | `["./src/app/**/*.{ts,tsx}", "./src/components/**/*.{ts,tsx}", "./emails/**/*.{ts,tsx}"]` | Specifies the file paths to scan for Tailwind CSS class names. |
| `theme.extend` | `{ fontFamily: { sans: ["var(--font-sans)", "Arial", "sans-serif"] } }` | Extends the default Tailwind CSS theme to include a custom sans-serif font family. |

#### Notes

- Ensure that the paths in `content` are updated if the project structure changes.
- Custom font families should be defined in the CSS variables (`var(--font-sans)`) to ensure consistency across the application.

### tsconfig.json

#### Overview

# tsconfig.json Module Overview

This file configures the TypeScript compiler for our project. It sets compiler options that dictate how TypeScript code is transpiled and how the compiler behaves during the build process.

#### Dependencies

- **TypeScript**: The primary dependency, used for type-checking and transpiling TypeScript code to JavaScript.

#### Configuration

| Option | Purpose | Details |
| --- | --- | --- |
| `target` | Specifies ECMAScript target version | `ES2017` |
| `lib` | Library files to include in the compilation | `["dom", "dom.iterable", "esnext"]` |
| `allowJs` | Allows JavaScript files to be compiled | `false` |
| `skipLibCheck` | Skips type checking of all declaration files | `true` |
| `strict` | Enables all strict type-checking options | `true` |
| `noEmit` | Prevents emitting output files | `true` |
| `esModuleInterop` | Enables interoperability between CommonJS and ES Modules | `true` |
| `module` | Specifies module code generation | `esnext` |
| `moduleResolution` | Module resolution preference | `bundler` |
| `resolveJsonModule` | Allows importing `.json` files directly | `true` |
| `isolatedModules` | Treats each file as a separate module | `true` |
| `jsx` | JSX code generation option | `preserve` |
| `incremental` | Enables incremental compilation | `true` |
| `plugins` | Custom compiler plugins | `[{ "name": "next" }]` |
| `paths` | Module path mappings | `{ "@/*": ["./src/*"] }` |

#### Notes

- `noEmit: true` means this configuration is used for type checking only and not for producing JavaScript files.
- `isolatedModules: true` ensures that each file is treated as a separate module, which can help with tree-shaking and module bundling.
- `jsx: preserve` keeps the JSX syntax in the output without transforming it, useful for frameworks like Next.js.
- `paths` mapping (`@/*`) allows for cleaner imports by using `@` as a shortcut for the `src` directory.

### emails/AdminAlert.tsx

#### Overview

# Module Overview

The `emails/AdminAlert.tsx` file defines a React component for generating an email notification to administrators when a delivery fails for an order. This email includes details about the failed order, the error encountered, and a list of products involved.

# Dependencies

| Import | Purpose |
| --- | --- |
| `@react-email/components` | Provides React components for building email templates. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `AdminAlertEmail` | Generates an email template for failed order deliveries. | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `AdminAlertEmail` | `orderId: string`, `error: string`, `products: Array<{ name: string; repoUrl: string }>` | JSX for email template | Renders an email with details about a failed order delivery. |

# Configuration

No specific configurations are defined in this file. The styles are hardcoded within the component.

# Notes

- The `Link` component from `@react-email/components` is used to create clickable links within the email. Ensure that the `repoUrl` provided is a valid URL.
- The `products` array should contain objects with `name` and `repoUrl` properties. The `repoUrl` is used as a key in the list, so it must be unique for each product.
- The email template is styled using inline styles defined in the `styles` object. Modify these styles to match the desired email appearance.

### emails/CollabInvite.tsx

#### Overview

# Module Overview

The `emails/CollabInvite.tsx` file generates an HTML email template for notifying users that their GitHub collaboration invite has been sent. It includes the buyer's name and a list of products with links to their respective GitHub repositories.

# Dependencies

| Import | Purpose |
| --- | --- |
| `@react-email/components` | Provides React components for building email templates. |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `CollabInviteEmail` | `buyerName: string` <br> `products: Array<{ name: string; repoUrl: string }>` | JSX.Element | Renders the email template with the buyer's name and product links. |

# Configuration

The email template uses a set of predefined styles to ensure consistency and readability. These styles are defined in the `styles` constant.

# Notes

- Ensure that the `buyerName` and `products` parameters are always provided when using the `CollabInviteEmail` function.
- The `products` array should contain objects with `name` and `repoUrl` properties.
- The `Link` component from `@react-email/components` is used to create clickable links within the email.

### emails/PurchaseConfirmation.tsx

#### Overview

# Module Overview

The `emails/PurchaseConfirmation.tsx` file generates a purchase confirmation email for customers who have made a purchase. It uses React components to structure the email content and includes styles for a clean, professional look.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `@react-email/components` | Provides React components specifically designed for email templates. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `PurchaseConfirmationEmail` | `buyerName: string`, `products: Array<{ name: string; repoUrl: string }>`, `orderId: string` | JSX Element | Renders the purchase confirmation email with the buyer's name, product details, and order ID. |

#### Configuration

| Variable | Purpose |
| --- | --- |
| `styles` | Contains CSS styles for the email components, ensuring a consistent look across all emails. |

#### Notes

- The `products` array should contain objects with `name` and `repoUrl` properties.
- Ensure the `buyerName` and `orderId` are sanitized to prevent XSS attacks.
- The `Link` component from `@react-email/components` is used to create clickable links within the email.

### prisma/schema.prisma

#### Overview

# Module Overview

The `prisma/schema.prisma` file defines the schema for our database using Prisma. It outlines the structure of our data, including tables, relationships, and constraints. This schema is used by the Prisma Client to interact with the PostgreSQL database.

#### Dependencies

| Import | Purpose |
|--------|---------|
| `prisma-client-js` | Provides the Prisma Client for interacting with the database. |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `User` | Represents a user in the system. | N/A |
| `Account` | Represents an account linked to a user. | N/A |
| `Session` | Represents a user session. | N/A |
| `VerificationToken` | Represents a token used for email verification. | N/A |
| `Product` | Represents a product available for purchase. | N/A |
| `Order` | Represents an order placed by a user. | N/A |
| `OrderItem` | Represents an item within an order. | N/A |
| `WebhookEvent` | Represents a webhook event received from an external provider. | N/A |

#### Functions

There are no explicit functions defined in this schema file.

#### Configuration

| Key | Value |
|-----|-------|
| `generator client` | Specifies the Prisma Client generator. |
| `provider` | Sets the provider to `prisma-client-js`. |
| `datasource db` | Configures the database connection. |
| `provider` | Sets the database provider to `postgresql`. |
| `url` | Specifies the database URL from the environment variable `DATABASE_URL`. |

#### Notes

- The `@id` attribute denotes the primary key for a model.
- The `@unique` attribute ensures that the field values are unique across the table.
- The `@default` attribute sets a default value for a field.
- The `@relation` attribute defines relationships between models.
- The `@updatedAt` attribute automatically updates the timestamp when the record is modified.
- The `@db.Text` attribute is used for long text fields in PostgreSQL.
- The `Json` type is used for JSON data in the `WebhookEvent` model.
- The `cuid()` function generates a unique identifier.

Understanding these annotations and configurations is crucial for maintaining and extending the database schema.

### prisma/seed.ts

#### Overview

# prisma/seed.ts

This file initializes the database with sample data using Prisma.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `PrismaClient` | Provides an interface to interact with the database. |
| `sampleProducts` | Contains sample product data to seed the database. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `main` | None | Promise<void> | Seeds the database with sample product data. |

#### Notes

- The `main` function iterates over the `sampleProducts` array and upserts each product into the database.
- The `upsert` method ensures that each product is either updated if it exists or created if it doesn't.
- The `prisma.$disconnect()` method is called to close the database connection after the seeding process.
- Errors during the seeding process will log the error and exit the process with a status code of 1.

### src/middleware.ts

#### Overview

# Module Overview

This file defines middleware for a Next.js application that enforces session-based authentication for specific routes. It checks for the presence of session cookies and redirects unauthenticated users to the sign-in page.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `NextResponse`, `NextRequest` | Provides response and request objects for Next.js middleware. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `hasSessionCookie` | `request: NextRequest` | `boolean` | Checks if the request contains any valid session cookies. |
| `middleware` | `request: NextRequest` | `NextResponse` | Redirects unauthenticated users to the sign-in page if they attempt to access protected routes. |

#### Configuration

| Constant | Purpose |
| --- | --- |
| `protectedPrefixes` | List of URL prefixes that require authentication. |

#### Notes

- The middleware redirects users to `/auth/signin` with the intended path appended as a query parameter.
- The `config` object specifies which routes this middleware applies to.

### src/app/error.tsx

#### Overview

# src/app/error.tsx

#### Module Overview

This file defines a React component `ErrorPage` that renders a user-friendly error message when an application request fails. It provides a way to reset the failed request and navigate back to the home page.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` from `next/link` | For client-side navigation. |
| `Button` from `@/components/ui/Button` | A custom button component for consistent UI. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ErrorPage` | Displays an error message and actions to retry or navigate home. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ErrorPage` | `reset` | `void` | A function to reset the failed request. |
| `error` | `Error & { digest?: string }` | `Error` | An error object with an optional digest. |

#### Notes

- The `ErrorPage` component displays a clear message about the failure and provides two actions: retrying the request and navigating to the home page.
- The `reset` function is called when the retry button is clicked.
- The `Link` component uses client-side navigation to avoid a full page reload when redirecting to the home page.

### src/app/globals.css

#### Overview

# src/app/globals.css

#### Module Overview

`src/app/globals.css` is the central stylesheet for our application, setting up global styles and variables that are used throughout the project. It ensures consistency in design and appearance across all components.

#### Dependencies

- **tailwindcss**: A utility-first CSS framework that provides a set of classes to style the application.

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `:root` | Defines global CSS variables for colors, fonts, and other reusable values. | N/A |
| `*` | Applies `box-sizing: border-box` to all elements. | N/A |
| `html` | Ensures smooth scrolling behavior. | N/A |
| `body` | Sets the minimum height, margin, background color, text color, and font family for the entire page. | N/A |
| `a` | Removes default text decoration and inherits text color. | N/A |
| `img` | Ensures images are displayed as blocks and do not exceed their container width. | N/A |
| `button, input, textarea, select` | Ensures these elements inherit the font from their parent. | N/A |
| `::selection` | Changes the background and text color when text is selected. | N/A |
| `.content-grid` | Centers and limits the width of the content grid. | N/A |
| `.prose-lite p` | Styles paragraphs within the `.prose-lite` class with specific margins and line height. | N/A |

#### Notes

- The CSS variables defined in `:root` are used throughout the stylesheet to maintain consistency and ease of updates.
- The `min-height: 100vh` on the `body` ensures the page takes up at least the full viewport height.
- The `scroll-behavior: smooth` on `html` provides a smooth scrolling effect when navigating to anchor links.
- The `.content-grid` class uses a responsive width to ensure the content is centered and does not exceed a maximum width of 1120px.

### src/app/layout.tsx

#### Overview

# src/app/layout.tsx

#### Module Overview

This file defines the root layout component for the application. It wraps the entire application in a standard HTML structure and includes the `Navbar` and `Footer` components, ensuring a consistent look and feel across all pages.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type definition for metadata configuration. |
| `Navbar` | Navigation bar component. |
| `Footer` | Footer component. |
| `globals.css` | Global styles for the application. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `RootLayout` | `{ children: React.ReactNode }` | `JSX.Element` | Renders the root layout with `Navbar`, `children`, and `Footer`. |

#### Configuration

| Variable | Purpose |
| --- | --- |
| `metadata` | Defines metadata and OpenGraph tags for the application. |

#### Notes

- The `metadata` object is used to set the default title and description for the application, as well as OpenGraph tags for social media sharing.
- The `RootLayout` component is responsible for rendering the `html` and `body` tags, including the `Navbar` and `Footer` components.
- The `children` prop is used to render any nested components within the `RootLayout`.

### src/app/loading.tsx

#### Overview

# `src/app/loading.tsx`

**Module Overview**

This file defines a `Loading` component that displays a loading indicator when the application is fetching data. It provides a visual placeholder to inform users that content is being loaded.

**Dependencies**

- **React**: The core library for building the component.

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Loading` | None | JSX element | Renders a loading indicator with a circular progress bar and three placeholders for additional content |

**Configuration or Constants**

- None

**Notes**

- The `Loading` component uses Tailwind CSS classes for styling.
- The loading indicator is a simple visual placeholder and does not interact with any state or props.
- This component should be used conditionally based on the application's loading state.

### src/app/not-found.tsx

#### Overview

# Module Overview

This file defines the `NotFound` component, which renders a 404 error page when a requested resource is not found. It provides a user-friendly message and a navigation link to the main product catalog.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | Component from Next.js for client-side navigation. |
| `Button` | UI component for rendering buttons, imported from the UI component library. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `NotFound` | N/A | JSX element | Renders the 404 error page with a message and a button to navigate to the product catalog. |

#### Notes

- The `NotFound` component uses a styled `div` to layout the error message and a button.
- The button is wrapped in a `Link` component to navigate to the product catalog without a full page reload.
- The color for the 404 text is defined using CSS variables for easy theming.

### src/app/page.tsx

#### Overview

# src/app/page.tsx

#### Module Overview

This file defines the `HomePage` component, which is the main entry point for the homepage of our application. It fetches featured products and displays them along with other static content like a hero section and a brief description of the service.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` from `next/link` | Provides client-side navigation capabilities. |
| `Button` from `@/components/ui/Button` | Custom button component for consistent UI. |
| `ProductCard` from `@/components/products/ProductCard` | Component to display individual product information. |
| `getFeaturedProducts` from `@/lib/products` | Function to fetch the list of featured products. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `HomePage` | None | JSX | The main function that returns the homepage component. |

#### Notes

- The `HomePage` function is an `async` function because it needs to fetch data before rendering the page.
- The `getFeaturedProducts` function is awaited to ensure the data is loaded before the component is rendered.
- The layout and styling are heavily dependent on Tailwind CSS classes.
- Ensure that the `getFeaturedProducts` function handles errors gracefully to avoid runtime issues.

### src/app/robots.ts

#### Overview

# Module Overview

The `src/app/robots.ts` file defines the configuration for the robots.txt file, which is used to communicate with web crawlers about which pages on the site should not be crawled or indexed. This file is essential for controlling search engine indexing behavior.

#### Dependencies

- **MetadataRoute**: Imported from `next`, this type defines the structure for the robots metadata.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `robots` | None | `MetadataRoute.Robots` | Generates the robots.txt configuration. |

#### Notes

- The `baseUrl` is determined by the `NEXT_PUBLIC_APP_URL` environment variable or defaults to `http://localhost:3000`.
- The `robots` function specifies that all user agents can crawl the root path (`/`) but should not crawl `/admin`, `/api`, or `/dashboard`.
- The sitemap URL is constructed using the `baseUrl`.

### src/app/sitemap.ts

#### Overview

# `src/app/sitemap.ts`

#### Module Overview

This file generates a sitemap for the application. It constructs URLs for the static routes and dynamic product routes, providing them with a `lastModified` timestamp.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `MetadataRoute` | Type definition for Next.js sitemap metadata. |
| `getProducts` | Function to fetch product data from the product service. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `sitemap` | None | `Promise<MetadataRoute.Sitemap>` | Generates a sitemap with URLs for static routes and dynamic product routes. |

#### Notes

- The `sitemap` function uses environment variable `NEXT_PUBLIC_APP_URL` to construct base URLs.
- The `lastModified` timestamp for each URL is set to the current date.
- The `getProducts` function is awaited to ensure product data is fetched before constructing the sitemap.

### src/app/admin/page.tsx

#### Overview

# Module Overview

The `src/app/admin/page.tsx` file defines the main page for the admin section of the application. It displays key statistics and provides navigation links for managing products and orders.

#### Dependencies

| Import | Description |
| ------ | ----------- |
| `Link` | From `next/link`, used for client-side navigation. |
| `Metadata` | Type from `next`, used for setting page metadata. |
| `Button` | Custom button component from the UI library. |
| `requireAdmin` | Guard function to ensure the user is an admin. |
| `getAdminStats` | Function to fetch admin statistics. |
| `formatMoney` | Utility function to format monetary values. |

#### Classes

| Class | Purpose | Key Methods |
| ----- | ------- | ----------- |
| `Stat` | Displays a statistic in a styled container. | None |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `AdminPage` | None | JSX Element | Renders the admin dashboard with statistics and navigation. |
| `Stat` | `{ label: string; value: string | number }` | JSX Element | Displays a statistic with a label and value. |

#### Notes

- The `AdminPage` function ensures that only admin users can access the page by calling `requireAdmin`.
- The `getAdminStats` function is awaited to fetch the necessary statistics before rendering the page.
- The `Stat` component is used to display various statistics in a consistent format.
- Ensure that the `requireAdmin` and `getAdminStats` functions are properly implemented and secured.

### src/app/admin/orders/page.tsx

#### Overview

# `src/app/admin/orders/page.tsx`

#### Module Overview

This file defines the `AdminOrdersPage` component, which displays a list of all orders for admin users. It fetches the orders, checks if the user is an admin, and renders the order details in a grid layout.

#### Dependencies

| Import | Description |
| --- | --- |
| `Metadata` | Type definition for Next.js metadata. |
| `StatusPill` | Component to display the status of an order. |
| `requireAdmin` | Function to ensure the user is an admin. |
| `getAllOrders` | Function to fetch all orders. |
| `formatMoney` | Function to format monetary values. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `AdminOrdersPage` | None | JSX | Main function that renders the admin orders page. |

#### Notes

- The `requireAdmin` function is called at the beginning of `AdminOrdersPage` to ensure that only admin users can access this page.
- The `getAllOrders` function is an async function that fetches all orders from the database.
- The `formatMoney` function is used to format the total amount of each order in a readable format.
- If there are no orders, a message "No orders yet." is displayed.
- The `StatusPill` component is used twice for each order to display the order status and collaboration status.

### src/app/admin/products/page.tsx

#### Overview

# Module Overview

The `src/app/admin/products/page.tsx` file defines the `AdminProductsPage` component, which is the main interface for managing products in the admin section of the application. It lists all products, including inactive ones, and provides options to edit or delete them.

#### Dependencies

| Import | Description |
|--------|-------------|
| `Link` | From `next/link`, used for client-side navigation. |
| `Metadata` | From `next`, used to define page metadata. |
| `DeleteProductButton` | Custom component for deleting a product. |
| `Button` | Custom button component for UI. |
| `Badge` | Custom component for displaying status badges. |
| `requireAdmin` | Guard function to ensure the user is an admin. |
| `getProducts` | Function to fetch product data. |
| `formatMoney` | Utility function to format currency. |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `AdminProductsPage` | None | JSX | Renders the admin products page, fetching and displaying product data. |

#### Notes

- The `requireAdmin` function must be called before any admin-specific logic to ensure the user is authorized.
- The `getProducts` function fetches all products, including inactive ones, which is essential for the admin's overview.
- The `formatMoney` function is used to format the product price in a user-friendly way.
- The `DeleteProductButton` component is a custom component for handling product deletions, encapsulating the delete logic.

### src/app/admin/products/[id]/edit/page.tsx

#### Overview

# Module Overview

This file defines the React component for the admin product edit page. It fetches product details based on the provided `id` and renders a form to update the product information.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type definition for metadata. |
| `notFound` | Redirects to a 404 page if the product does not exist. |
| `ProductForm` | Component to display the form for editing product details. |
| `requireAdmin` | Middleware to ensure the user has admin privileges. |
| `getProductById` | Function to fetch product details by ID. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `EditProductPage` | `{ params: Promise<{ id: string }> }` | JSX element | Renders the product edit page, fetches product data, and displays the `ProductForm`. |

#### Notes

- Ensure the `id` parameter is valid and exists in the database.
- If the product is not found, the `notFound` function will handle the redirection.
- The `requireAdmin` middleware ensures only admin users can access this page.
- The `ProductForm` component handles form submission and updates the product in the backend.

### src/app/admin/products/new/page.tsx

#### Overview

# Module Overview

This file defines the `NewProductPage` component for the admin section of the application. It renders a form for creating new products and ensures that only authenticated admin users can access this page.

# Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type definition for metadata properties. |
| `ProductForm` | Component for rendering the product creation form. |
| `requireAdmin` | Function to enforce admin access control. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `NewProductPage` | Main component for the new product page. | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `requireAdmin` | N/A | Promise<void> | Ensures the user is an admin. Redirects if not. |

# Notes

- The `requireAdmin` function is called at the top level of the component to enforce admin access.
- The `ProductForm` component is used to render the form for creating new products.
- The metadata for the page is set to "New product" for SEO purposes.

### src/app/api/admin/products/route.ts

#### Overview

# Module Overview

The `src/app/api/admin/products/route.ts` file defines the API routes for managing products in the admin section of the application. It provides endpoints for retrieving and creating product entries, ensuring that only authenticated admin users can access these functionalities.

#### Dependencies

| Import | Description |
| :--- | :--- |
| `NextResponse` | Provides a way to create responses in Next.js API routes. |
| `auth` | Handles user authentication and session management. |
| `hasDatabaseUrl` | Checks if the database URL is configured in the environment. |
| `fromDbProduct` | Converts a database product record to a format suitable for API responses. |
| `prisma` | ORM client for interacting with the database. |
| `productInputSchema` | Validates the input for creating or updating a product. |

#### Functions

| Function | Parameters | Returns | Description |
| :--- | :--- | :--- | :--- |
| `requireAdminResponse` | None | `NextResponse` | Ensures the user is authenticated and has admin privileges, returning an appropriate response if not. |
| `productData` | `input` (parsed product input) | `object` | Maps parsed product input to a format suitable for database insertion. |
| `GET` | None | `NextResponse` | Retrieves all products from the database and returns them in a JSON response. |
| `POST` | `request` (incoming HTTP request) | `NextResponse` | Creates a new product in the database based on the validated request payload. |

#### Notes

- The `requireAdminResponse` function is used to enforce admin access control for both `GET` and `POST` requests.
- The `productInputSchema` ensures that the product data is valid before attempting to create a new product.
- The `fromDbProduct` function is used to format database records into a consistent shape for API responses.
- Error handling is done through `NextResponse.json` with appropriate HTTP status codes.

### src/app/api/admin/products/[id]/route.ts

#### Overview

# Module Overview

This file defines the API routes for managing products in the admin section of the application. It handles HTTP requests for retrieving, updating, and deleting product data, ensuring that only authenticated admin users can perform these actions.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `NextResponse` | Provides methods to create HTTP responses in a Next.js API route. |
| `auth` | Handles user authentication and session management. |
| `hasDatabaseUrl` | Checks if the database URL is configured in the environment variables. |
| `fromDbProduct` | Converts a database product object to a format suitable for API responses. |
| `prisma` | ORM client for interacting with the database. |
| `productInputSchema` | Validates the input for product operations. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `requireAdminResponse` | None | `NextResponse` | Ensures the user is authenticated and has admin privileges. Returns an error response if conditions are not met. |
| `productData` | `input` (parsed product input) | `object` | Constructs a product data object from parsed input. |
| `GET` | `_request` (Request), `{ params }` (Promise<{ id: string }>) | `NextResponse` | Retrieves a product by ID and returns it in the response. |
| `PUT` | `request` (Request), `{ params }` (Promise<{ id: string }>) | `NextResponse` | Updates a product by ID with new data and returns the updated product. |
| `DELETE` | `_request` (Request), `{ params }` (Promise<{ id: string }>) | `NextResponse` | Deletes a product by ID or sets it as inactive if deletion fails. |

#### Notes

- The `requireAdminResponse` function is a middleware that checks for admin access and returns an appropriate error response if the user is not authorized.
- The `productData` function maps the parsed product input to a format that can be used to update the database.
- Error handling is implemented in each route to ensure that invalid requests or missing data result in clear error messages.
- The `fromDbProduct` function is used to format the product data retrieved from the database for API responses.
- The `prisma` client is used for all database interactions, ensuring that the data layer is consistent and efficient.

### src/app/api/auth/[...nextauth]/route.ts

#### Overview

# Module Overview

This file defines the API route for authentication in the NextAuth system. It exports the necessary handlers for GET and POST requests to manage authentication processes.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `handlers` | Provides the GET and POST request handlers for authentication. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `GET` | Request, Response | Promise<Response> | Handles GET requests for authentication. |
| `POST` | Request, Response | Promise<Response> | Handles POST requests for authentication. |

#### Notes

- The `runtime` is set to `"nodejs"` to ensure the API route runs in a Node.js environment.
- The handlers are directly imported and exported for use in the API route, simplifying the integration with Next.js routing.

### src/app/api/orders/[id]/retry-collab/route.ts

#### Overview

# Module Overview

This file defines the API route for retrying a collaboration on an order. When an order has a collaboration issue, this endpoint allows the user to retry the collaboration process.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `CollabStatus` | Enum for collaboration statuses from the Prisma client. |
| `NextResponse` | Utility from Next.js for creating HTTP responses. |
| `auth` | Authentication middleware to verify user sessions. |
| `fulfillPaidOrder` | Function to handle the fulfillment of a paid order. |
| `hasDatabaseUrl` | Utility to check if the database URL is configured. |
| `prisma` | Prisma client for database interactions. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `POST` | `_request: Request`, `{ params }: { params: Promise<{ id: string }> }` | `NextResponse` | Handles POST requests to retry a collaboration on an order. |

### Function Details

#### `POST`

- **Parameters**
  - `_request: Request`: The incoming HTTP request.
  - `{ params }: { params: Promise<{ id: string }> }`: URL parameters containing the order ID.

- **Returns**
  - `NextResponse`: JSON response indicating the result of the retry operation.

- **Description**
  - Authenticates the user.
  - Checks if the database is configured.
  - Retrieves the order based on the provided ID and user.
  - Verifies the order is paid and updates its collaboration status.
  - Fulfills the paid order.
  - Returns a success response if the retry is successful.

#### Notes

- Ensure the database URL is configured before using this endpoint.
- Only authenticated users can access this route.
- The order must be in the "PAID" status to be retried.
- The `fulfillPaidOrder` function is called to handle the order fulfillment after updating the collaboration status.

### src/app/api/orders/create/route.ts

#### Overview

# Module Overview

The `src/app/api/orders/create/route.ts` file defines the API route for creating orders. It handles the creation of an order, validates the input, checks product availability, and initiates the payment process using either Stripe, Razorpay, or a mock provider if configured.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `CollabStatus, OrderStatus` | Enums from Prisma client for collaboration and order statuses. |
| `NextResponse` | Utility from Next.js to create HTTP responses. |
| `auth` | Authentication middleware to verify user sessions. |
| `canUseMockPayments, hasDatabaseUrl, selectedPaymentProvider` | Environment variable utilities. |
| `prisma` | Prisma client for database operations. |
| `createOrderSchema` | Validation schema for order creation. |
| `createStripeCheckoutSession` | Function to create a Stripe checkout session. |
| `createRazorpayOrder` | Function to create a Razorpay order. |
| `absoluteUrl` | Utility to generate absolute URLs. |
| `fromDbProduct` | Function to transform product data from the database. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `POST` | `request: Request` | `NextResponse` | Handles the POST request to create an order, validates input, checks product availability, and initiates the payment process. |

#### Notes

- The function checks if the user is authenticated before proceeding with order creation.
- Ensures that the `DATABASE_URL` environment variable is set to allow order creation.
- Validates the order payload using the `createOrderSchema`.
- Checks if all products are available and if they share the same currency.
- Initiates the payment process based on the selected payment provider.
- If mock payments are enabled, it updates the order status to paid and returns a mock success URL.

### src/app/api/webhooks/razorpay/route.ts

#### Overview

# src/app/api/webhooks/razorpay/route.ts

#### Module Overview

This file handles Razorpay webhook events, ensuring that payment and order updates are processed correctly within our application.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `crypto` | Provides cryptographic functionality to generate unique identifiers. |
| `NextResponse` | Used to create HTTP responses in a Next.js API route. |
| `hasDatabaseUrl` | Checks if the database URL is configured. |
| `markOrderPaidAndFulfill` | Marks an order as paid and fulfills it. |
| `prisma` | ORM client for interacting with the database. |
| `verifyRazorpayWebhookSignature` | Verifies the signature of Razorpay webhook requests. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `POST` | `request: Request` | `Promise<NextResponse>` | Handles incoming Razorpay webhook requests. |

#### Notes

- The `POST` function checks if the database is configured and verifies the Razorpay webhook signature.
- It processes specific events (`payment.captured` and `order.paid`) by marking orders as paid and fulfilled.
- The function logs events in the database and handles errors gracefully.

### src/app/api/webhooks/stripe/route.ts

#### Overview

# `src/app/api/webhooks/stripe/route.ts` Module Overview

This file handles incoming webhook events from Stripe, processes them, and updates the application's state accordingly. It's a crucial part of integrating Stripe's payment processing into our application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Stripe` | Stripe's official library for interacting with the Stripe API. |
| `NextResponse` | A utility from Next.js for creating HTTP responses. |
| `hasDatabaseUrl` | A utility function to check if the database URL is configured. |
| `markOrderPaidAndFulfill` | A function to mark an order as paid and fulfill it. |
| `markOrderRefundedAndRevoke` | A function to mark an order as refunded and revoke it. |
| `prisma` | An instance of Prisma Client for interacting with the database. |
| `constructStripeEvent` | A utility function to construct a Stripe event from the raw webhook payload. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `POST` | `request: Request` | `NextResponse` | Handles POST requests to the webhook endpoint, processes Stripe events, and updates the application state. |

#### Notes

- The `POST` function checks if the database is configured before processing any webhook events.
- It uses the `constructStripeEvent` function to validate the incoming Stripe webhook payload.
- The function updates the application state based on the type of Stripe event received.
- It handles events like `checkout.session.completed`, `checkout.session.async_payment_failed`, and `charge.refunded`.
- The function logs the webhook events in the database and marks them as processed or failed.
- If an error occurs during processing, it updates the webhook event with the error message.

### src/app/auth/signin/page.tsx

#### Overview

# src/app/auth/signin/page.tsx

#### Module Overview

This file defines the `SignInPage` component for the application's sign-in functionality. It handles the rendering of the GitHub OAuth login page and integrates the `SignInButton` component to facilitate user authentication.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type definition for metadata used in Next.js pages. |
| `SignInButton` | Component for rendering the GitHub OAuth sign-in button. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `SignInPage` | `searchParams: Promise<{ callbackUrl?: string }>` | JSX element | Renders the sign-in page with GitHub OAuth login button. |

#### Configuration or Constants

| Name | Type | Description |
| --- | --- | --- |
| `metadata` | `Metadata` | Page metadata for SEO, sets the page title to "Sign in". |

#### Notes

- The `callbackUrl` parameter is optional and defaults to `/dashboard` if not provided.
- The `SignInButton` component is responsible for initiating the GitHub OAuth flow.
- Ensure that the `searchParams` promise resolves correctly to include the `callbackUrl` if needed for future redirections.

### src/app/checkout/page.tsx

#### Overview

# src/app/checkout/page.tsx

#### Module Overview

This file defines the `CheckoutPage` component, which is the main entry point for the checkout process in our application. It handles fetching product details, displaying available products if no specific product is selected, and rendering the payment form for order completion.

#### Dependencies

| Import | Description |
| --- | --- |
| `Link` | From `next/link`, used for client-side navigation. |
| `Metadata` | From `next`, used for setting page metadata. |
| `PaymentForm` | Custom component for handling payment details. |
| `Button` | Custom UI button component. |
| `getProductById`, `getProducts` | Functions to fetch product data. |
| `CatalogProduct` | Type definition for catalog products. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `CheckoutPage` | `{ searchParams: Promise<{ product?: string | string[] }> }` | `JSX.Element` | Renders the checkout page, fetching and displaying products or the payment form. |

#### Notes

- The `CheckoutPage` function is an `async` function that fetches product details based on the `searchParams`.
- If no products are found, it displays a list of available products for the user to choose from.
- The `PaymentForm` component is only rendered if products are available.
- Ensure that `getProductById` and `getProducts` functions handle errors gracefully to avoid breaking the checkout flow.

### src/app/checkout/failure/page.tsx

#### Overview

# src/app/checkout/failure/page.tsx

#### Module Overview

This file defines the `CheckoutFailurePage` component, which renders the checkout failure page when a payment attempt fails. It provides a user-friendly message and navigation options to either try again or return home.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | A Next.js component for client-side navigation. |
| `Metadata` | A type for defining metadata for a page. |
| `Button` | A UI component for rendering buttons. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `CheckoutFailurePage` | Renders the checkout failure page. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `CheckoutFailurePage` | None | JSX element | Renders a div with a message and two buttons for navigation. |

#### Configuration

| Identifier | Purpose |
| --- | --- |
| `metadata` | Defines the page metadata with a title indicating payment failure. |

#### Notes

- The `Button` component is used to create navigation buttons with `asChild` prop to wrap `Link` components.
- The page provides clear instructions and navigation options for users who have failed to complete a payment.

### src/app/checkout/success/page.tsx

#### Overview

# src/app/checkout/success/page.tsx

#### Module Overview

This file defines the `CheckoutSuccessPage` component, which is displayed after a successful checkout. It shows a confirmation message, displays the order status, and provides links to navigate to the dashboard or browse more products.

#### Dependencies

| Import | Purpose |
| ------ | ------- |
| `Link` | From `next/link`, used for client-side navigation. |
| `Metadata` | From `next`, used to define page metadata. |
| `Button` | Custom UI component for rendering buttons. |
| `StatusPill` | Custom UI component for displaying status indicators. |
| `getOrderById` | Function to fetch order details by ID. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `CheckoutSuccessPage` | `searchParams: Promise<{ orderId?: string; mock?: string; provider?: string }>` | JSX Element | Renders the checkout success page with order details and status. |

#### Notes

- The `searchParams` prop is a promise that resolves to an object containing `orderId`, `mock`, and `provider`.
- If `orderId` is present, the function fetches the order details using `getOrderById`.
- The page displays different messages based on the presence of `mock` and `provider`.
- Ensure the `getOrderById` function handles cases where the order ID is invalid or the order does not exist.

### src/app/dashboard/page.tsx

#### Overview

# src/app/dashboard/page.tsx

#### Module Overview

This file defines the `DashboardPage` component, which is the main entry point for the user's dashboard. It displays the user's purchased repositories and recent orders, providing a summary of their activity.

#### Dependencies

| Import | Description |
| ------ | ----------- |
| `Link` | From `next/link`, used for navigation. |
| `Metadata` | From `next`, used for setting page metadata. |
| `OrderList` | Custom component to display a list of orders. |
| `PurchasedRepos` | Custom component to display purchased repositories. |
| `Button` | Custom UI button component. |
| `requireUser` | Utility function to ensure a user is authenticated. |
| `getOrdersForUser` | Utility function to fetch orders for a user. |

#### Functions

| Function | Parameters | Returns | Description |
| -------- | ---------- | ------- | ----------- |
| `DashboardPage` | None | JSX | Main component that renders the dashboard page. |

#### Notes

- The `DashboardPage` function is an asynchronous function that fetches user data and orders before rendering the page.
- The `requireUser` function ensures that the user is authenticated before accessing the dashboard.
- The `getOrdersForUser` function retrieves the orders for the authenticated user.
- The `Link` component is used for navigation within the application.
- The `OrderList` and `PurchasedRepos` components are custom components that display specific data related to the user's activity.
- The `Button` component is used for the "Browse products" button, providing a consistent UI element for navigation.

### src/app/dashboard/orders/page.tsx

#### Overview

# src/app/dashboard/orders/page.tsx

#### Module Overview

This file defines the React component for the dashboard's orders page, fetching and displaying a user's order history.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type for metadata configuration. |
| `OrderList` | Component to display a list of orders. |
| `requireUser` | Function to ensure the user is authenticated. |
| `getOrdersForUser` | Function to fetch orders for the authenticated user. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `DashboardOrdersPage` | None | JSX element | Main component for the orders page. Fetches user data and orders, then renders the `OrderList` component. |

#### Notes

- The `requireUser` function is used to check if the user is authenticated before fetching their orders.
- The `getOrdersForUser` function fetches the orders for the authenticated user's ID.
- The `OrderList` component is used to display the fetched orders in a grid format.
- The metadata for the page is set with a title "Orders".

### src/app/dashboard/orders/[id]/page.tsx

#### Overview

# `src/app/dashboard/orders/[id]/page.tsx`

This file displays the details of a specific order for a user on the dashboard. It fetches the order data, checks if the order exists, and renders the order details including items and status.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | Provides navigation capabilities. |
| `Metadata` | Defines metadata for the page. |
| `notFound` | Redirects to a not found page. |
| `RetryCollabButton` | A button to retry collaboration. |
| `StatusPill` | Displays the status of an order. |
| `Button` | A customizable button component. |
| `requireUser` | Ensures the user is authenticated. |
| `getOrderForUser` | Fetches the order for the user. |
| `formatMoney` | Formats a monetary value. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `DashboardOrderDetailPage` | `{ params: Promise<{ id: string }> }` | JSX | Renders the order detail page. |

#### Notes

- The page checks if the order exists and redirects to a not found page if it doesn't.
- The `order.items` array is mapped to display each item's details.
- The `RetryCollabButton` is conditionally rendered if the collaboration status is "FAILED".
- Ensure the `order` object has the expected structure before accessing its properties.

### src/app/products/page.tsx

#### Overview

# `src/app/products/page.tsx`

#### Module Overview

This file defines the main React component for the products catalog page. It fetches product data and renders a catalog of products using the `ProductCatalog` component.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type for metadata object used in Next.js |
| `ProductCatalog` | Component to display the product catalog |
| `getProducts` | Function to fetch product data |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `getProducts` | None | Promise<Array<Product>> | Fetches product data |

#### Notes

- The `metadata` object is used to set the page title for SEO purposes.
- The `ProductsPage` component fetches product data asynchronously and passes it to the `ProductCatalog` component.
- Ensure that error handling is added for the `getProducts` call to manage failed fetches gracefully.
- The layout uses Tailwind CSS classes for styling.

### src/app/products/[slug]/page.tsx

#### Overview

# Module Overview

This file defines the React component for displaying product details based on a product slug. It handles fetching product data from the backend and rendering a `ProductDetail` component with the fetched data.

# Dependencies

| Import | Purpose |
| --- | --- |
| `Metadata` | Type for metadata generation in Next.js. |
| `notFound` | Function to handle 404 responses in Next.js. |
| `ProductDetail` | Component to display product details. |
| `getProductBySlug` | Function to fetch product data by slug. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `generateMetadata` | `{ params: Promise<{ slug: string }> }` | `Promise<Metadata>` | Generates metadata for the product page based on the product slug. |
| `ProductDetailPage` | `{ params: Promise<{ slug: string }> }` | `JSX.Element` | Renders the product detail page. If the product is not found, it triggers a 404 response. |

# Notes

- The `generateMetadata` function fetches product data and constructs metadata including title, description, and Open Graph images.
- The `ProductDetailPage` function fetches product data and renders the `ProductDetail` component. If the product is not found, it triggers a 404 response using `notFound()`.
- Ensure that the `slug` provided in the parameters is valid and corresponds to an existing product in the database.

### src/components/admin/ProductActions.tsx

#### Overview

# Module Overview

This file defines a React component `DeleteProductButton` that allows an admin to delete a product from the system. It handles the confirmation dialog, the API call to delete the product, and updates the UI accordingly.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useRouter` | Hook from Next.js for navigation. |
| `useState` | Hook from React to manage state within functional components. |
| `Button` | A UI component for rendering buttons. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `remove` | None | None | Async function to delete a product. Shows a confirmation dialog and makes a DELETE request to the API. |

#### Notes

- The `remove` function uses `window.confirm` to get user confirmation before proceeding with the deletion.
- The `Button` component is disabled while the deletion process is in progress to prevent multiple submissions.
- The UI is updated by toggling the `isDeleting` state to show the deletion status.
- The `router.refresh` method is called upon successful deletion to update the product list.

### src/components/admin/ProductForm.tsx

#### Overview

# src/components/admin/ProductForm.tsx

#### Module Overview

This file defines the `ProductForm` component, which allows admins to create or edit product entries in the catalog. It handles form state, input changes, and submission to the backend API.

#### Dependencies

- `useRouter` from `next/navigation`: Provides routing capabilities.
- `useMemo`, `useState` from `react`: React hooks for state and memoization.
- `Button`, `Input`, `Textarea` from `@/components/ui`: UI components for form elements.
- `CatalogProduct` from `@/lib/sample-data`: Type definition for product data.
- `splitCsv`, `toSlug` from `@/lib/utils`: Utility functions for string manipulation.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `initialState` | `product?: CatalogProduct` | `ProductFormState` | Initializes form state based on provided product or defaults. |
| `update` | `key: Key`, `value: ProductFormState[Key]` | `void` | Updates a specific field in the form state. |
| `submit` | `()` | `Promise<void>` | Submits the form data to the backend API. |

#### Notes

- The `slug` field is auto-generated from the `name` field if no product is provided (i.e., when creating a new product).
- The `currency` field is always converted to uppercase.
- The `screenshots`, `tags`, and `techStack` fields expect comma-separated values, which are split into arrays before submission.
- On successful submission, the user is redirected to the product list page.

### src/components/auth/AuthButton.tsx

#### Overview

# Module Overview

This file defines two components for handling user authentication: `SignInButton` and `SignOutButton`. These components are used to manage user login and logout actions, redirecting users to specified URLs upon completion.

# Dependencies

| Import | Purpose |
| --- | --- |
| `signIn` | Function to initiate the sign-in process. |
| `signOut` | Function to initiate the sign-out process. |
| `Button` | UI component for rendering buttons. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `SignInButton` | `label` (optional, default: "Login with GitHub"), `callbackUrl` (optional, default: "/dashboard") | JSX element | Renders a form with a button to sign in, redirecting to `callbackUrl` upon success. |
| `SignOutButton` | N/A | JSX element | Renders a form with a button to sign out, redirecting to "/" upon success. |

# Notes

- The `SignInButton` component uses GitHub for authentication and redirects to the `callbackUrl` upon successful sign-in.
- The `SignOutButton` component redirects to the homepage ("/") upon successful sign-out.
- Both components use server-side actions to handle authentication, ensuring secure and immediate responses.

### src/components/checkout/PaymentForm.tsx

#### Overview

# PaymentForm.tsx

This file defines the `PaymentForm` component, which allows users to review their order and complete the payment process using either Stripe, Razorpay, or a mock provider.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useState` from `react` | Manages state for the form, including submission status and messages. |
| `Button` from `@/components/ui/Button` | A styled button component used for the form submission. |
| `CatalogProduct` from `@/lib/sample-data` | Type definition for the products being ordered. |
| `formatMoney` from `@/lib/utils` | Utility function to format monetary values. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `loadRazorpayScript` | None | `Promise<boolean>` | Loads the Razorpay script dynamically and resolves when the script is loaded. |

#### Notes

- The `PaymentForm` component expects an array of `CatalogProduct` objects as a prop, which it uses to display the order summary.
- The `handleSubmit` function handles the form submission, making an API call to create an order and then directing the user to the appropriate payment provider.
- Razorpay is loaded dynamically only when necessary, which helps with performance and security.
- Error handling is done through the `setMessage` state, which displays any errors to the user.
- The `Button` component is conditionally disabled based on the submission status and the presence of products.

### src/components/dashboard/OrderList.tsx

#### Overview

# src/components/dashboard/OrderList.tsx

#### Module Overview

This file renders a list of orders in the dashboard. It maps through an array of orders and displays each order's details, including its ID, items, status, and total amount.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | From `next/link` to create client-side navigation links. |
| `StatusPill` | A UI component to display status information. |
| `OrderWithItems` | Type definition for the order data structure. |
| `formatMoney` | Utility function to format monetary values. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `OrderList` | `{ orders: OrderWithItems[] }` | JSX element | Renders a list of orders with details like ID, items, status, and total amount. |

#### Notes

- The component checks if there are no orders and returns a message indicating this.
- Each order is rendered as a link to its detailed view.
- Monetary values are formatted using the `formatMoney` utility function.
- Status information is displayed using the `StatusPill` component.

### src/components/dashboard/PurchasedRepos.tsx

#### Overview

# Module Overview

This file defines a React component `PurchasedRepos` that displays a grid of repositories a user has purchased. It takes an array of orders, extracts the purchased items, and renders each item with its name, URL, and status.

# Dependencies

| Import | Purpose |
| --- | --- |
| `StatusPill` | A UI component to display status indicators. |
| `OrderWithItems` | Type definition for an order containing items. |

# Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `PurchasedRepos` | `orders` (`OrderWithItems[]`) | `JSX.Element` | Renders a grid of purchased repositories. |

# Notes

- The component checks if there are any paid items. If none, it displays a placeholder message.
- Each repository is rendered with its name, URL, and a status pill indicating its collaborative status.
- The URL for each repository opens in a new tab (`target="_blank"`).
- The `key` for each repository item is a combination of `order.id` and `product.id` to ensure unique keys for list rendering.

### src/components/dashboard/RetryCollabButton.tsx

#### Overview

# RetryCollabButton.tsx

#### Module Overview

This file defines a React component `RetryCollabButton` that allows users to retry collaboration for a specific order. It fetches data from the server to retry the collaboration process and displays a button to initiate the retry and feedback messages about the operation status.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `useState` | Hook to manage component state. |
| `Button` | UI component for rendering buttons. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| None | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `retry` | None | None | Initiates the retry process for the collaboration of a given order. |

#### Notes

- The `retry` function handles the API call to retry collaboration and updates the UI based on the response.
- The button is disabled while the retry process is in progress to prevent multiple submissions.
- Error messages are displayed if the retry fails or if there is an issue with the server response.

### src/components/layout/Footer.tsx

#### Overview

# src/components/layout/Footer.tsx

#### Module Overview

This file defines the `Footer` component, which renders the footer section of our application. It provides links to key parts of the site, including product browsing, checkout, and account management.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | From `next/link`, it's used for client-side navigation between pages. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Footer` | None | JSX element | Renders the footer with sections for company info, product catalog, and account links. |

#### Notes

- The footer uses Tailwind CSS classes for styling.
- The `Link` components are used for navigation without full page reloads.
- The text and links are static and may need updates if the site structure changes.

### src/components/layout/Navbar.tsx

#### Overview

# src/components/layout/Navbar.tsx

#### Module Overview

This file defines a `Navbar` component that renders the navigation bar for the application. It includes links to various parts of the site, user authentication buttons, and conditionally renders admin links based on user role.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` | From `next/link` for client-side navigation. |
| `auth` | From `@/lib/auth` to manage user sessions. |
| `SignInButton`, `SignOutButton` | From `@/components/auth/AuthButton` for user sign-in and sign-out functionalities. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Navbar` | None | JSX element | Renders the navigation bar, includes links and authentication buttons. |

#### Notes

- The `Navbar` component uses `auth` to check for user session and conditionally render links.
- The `SignOutButton` and `SignInButton` components handle user authentication.
- The `Link` components from `next/link` are used for navigation.
- The admin link is only shown if the user has an `ADMIN` role.

### src/components/products/ProductCard.tsx

#### Overview

# `ProductCard.tsx` Documentation

#### Module Overview

This file defines a `ProductCard` component that displays product details in a visually appealing card format. It takes a `CatalogProduct` object as input and renders an image, product name, description, tags, price, and a "Buy now" button.

#### Dependencies

| Import | Source | Purpose |
| --- | --- | --- |
| `Link` | `next/link` | Client-side navigation link component. |
| `Badge` | `@/components/ui/Badge` | Component to display tags. |
| `Button` | `@/components/ui/Button` | Button component with customizable size. |
| `CatalogProduct` | `@/lib/sample-data` | Type definition for product data. |
| `formatMoney` | `@/lib/utils` | Utility function to format product price. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `ProductCard` | Renders a product card with image, name, description, tags, price, and a "Buy now" button. | None |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ProductCard` | `{ product: CatalogProduct }` | JSX element | Renders a product card component. |

#### Notes

- The `product` prop is expected to have the following structure: `{ slug: string, imageUrl: string, tags: string[], name: string, description: string, price: number, currency: string, id: string }`.
- The `formatMoney` function is used to format the product price according to the specified currency.
- The "Buy now" button links to the checkout page with the product ID as a query parameter.
- The product image uses lazy loading for better performance.
- The product description is truncated to three lines using the `line-clamp-3` class.

### src/components/products/ProductCatalog.tsx

#### Overview

# Module Overview

The `ProductCatalog.tsx` file defines a React component that displays a catalog of products. It allows users to search and filter products by tags and tech stack.

# Dependencies

| Import | Purpose |
| --- | --- |
| `useMemo` | React hook to memoize values. |
| `useState` | React hook to manage component state. |
| `ProductCard` | Component to display individual product information. |
| `Input` | UI component for the search input field. |
| `CatalogProduct` | Type definition for product data. |

# Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ProductCatalog` | `{ products: CatalogProduct[] }` | JSX | Renders the product catalog with search and filter functionality. |

# Notes

- The component uses `useState` to manage the search query, selected tag, and selected tech stack.
- `useMemo` is used to compute unique tags and tech stacks from the product list, optimizing performance.
- The product list is filtered based on the search query, selected tag, and selected tech stack.
- If no products match the current filters, a message is displayed.

### src/components/products/ProductDetail.tsx

#### Overview

# ProductDetail.tsx

This file defines the `ProductDetail` component, which displays detailed information about a product, including its images, tags, description, price, and additional details.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `Link` from `next/link` | Provides client-side navigation between pages. |
| `Badge` from `@/components/ui/Badge` | Displays a small label or tag. |
| `Button` from `@/components/ui/Button` | Renders a clickable button. |
| `type CatalogProduct` from `@/lib/sample-data` | Defines the type for product data. |
| `formatMoney` from `@/lib/utils` | Formats a number as currency. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `ProductDetail` | `{ product: CatalogProduct }` | JSX Element | Renders the detailed view of a product. |

#### Notes

- The `product` prop must be an object conforming to the `CatalogProduct` type.
- The `formatMoney` function is used to format the product price.
- Ensure that `product.imageUrl` and `product.screenshots` are valid URLs for images to be displayed correctly.
- The `Button` component is conditionally rendered based on the presence of `product.demoUrl`.

### src/components/ui/Badge.tsx

#### Overview

# `Badge.tsx` Documentation

This file defines a reusable UI component for rendering a badge. A badge is a small piece of UI that typically displays a label or a count, often used to highlight important information or statuses.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `cn` | Utility function for conditionally combining class names. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `Badge` | `children: React.ReactNode`, `className?: string` | `JSX.Element` | Renders a styled badge component. |

#### Notes

- The `Badge` component accepts `children` for the content to display inside the badge.
- An optional `className` prop allows for additional CSS classes to be applied.
- The default styling includes a rounded, bordered, white background with neutral text color.

### src/components/ui/Button.tsx

#### Overview

# Button.tsx

#### Module Overview

This file defines a customizable `Button` component, leveraging Radix UI's `Slot` for flexible element usage and `class-variance-authority` for conditional class application.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `React` | Provides React functionality for building UI components. |
| `Slot` from `@radix-ui/react-slot` | Allows the button to render as a different HTML element if needed. |
| `cva` and `VariantProps` from `class-variance-authority` | Manages conditional class application based on props. |
| `cn` from `@/lib/utils` | Utility function to concatenate class names conditionally. |

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| `Button` | A customizable button component. | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `buttonVariants` | N/A | Configuration object | Defines the styling variants for the button. |

#### Configuration

- `buttonVariants`: Configures the button's appearance with different variants and sizes.

#### Notes

- The `Button` component uses `React.forwardRef` to forward the ref to the underlying element, which is useful for cases where the button needs to be focused programmatically.
- The `asChild` prop allows the button to render as a different HTML element, which is useful for wrapping the button in a link or another interactive element.
- Ensure to pass the `className` prop to customize the button's appearance further.
- The default variant for the button is `primary` with a medium size. Adjust these defaults as needed for your application's theme.

